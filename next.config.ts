import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["tailwindcss.com", "images.unsplash.com", "plus.unsplash.com", "upload.wikimedia.org", "media.istockphoto.com", "www.material-tailwind.com"],
  },
};

export default nextConfig;
